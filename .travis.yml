language: python
sudo: false

python:
  - "2.7"

notifications:
  email: false

before_install:
  #- apt-key adv --fetch-keys http://repos.codelite.org/CodeLite.asc
  #- sudo apt-add-repository 'deb http://repos.codelite.org/wx3.0.2/ubuntu/ precise universe'
  #- curl http://repos.codelite.org/wx3.0.2/ubuntu/ | sudo apt-key add -
  #- echo "deb http://apt.wxwidgets.org/ Precise-wx main" | sudo tee -a /etc/apt/sources.list
  #- echo "deb-src http://apt.wxwidgets.org/ Precise-wx main  " | sudo tee -a /etc/apt/sources.list
  #- sudo apt-get update -qq
  #- sudo apt-get install python-wxgtk2.8 python-wxtools wx2.8-i18n
  #- sudo apt-get install python-wxgtk2.8 python-wxtools wx2.8-doc wx2.8-examples wx2.8-headers wx2.8-i18n
  # figure out where we are
#  - ls
  # download the source tarball
#  - wget https://github.com/wxWidgets/wxPython/archive/wxPy-3.0.2.0.tar.gz
  # extract the tarbal
#  - ls
#  - tar -xzf wxPy-3.0.2.0.tar.gz
#  - ls
#  - cd wxPython-wxPy-3.0.2.0/wxPython
#  - python build-wxpython.py --build_dir=../bld --install

install:
  - pip install --upgrade --trusted-host wxpython.org --pre -f http://wxpython.org/Phoenix/snapshot-builds/ wxPython_Phoenix
  - pip install --upgrade pip
  - pip install -r dev-requirements.txt
  - pip install coveralls

script:
  #- coverage run -m ci_setup_check core.py test_core.py
  - nosetests --with-cov --cov ci_setup_check --cov-config .coverage.rc --logging-level=INFO
  #- nosetests --with-cov --cov ci_setup_check --logging-level=INFO

# Calculate coverage
after_success:
  #- coveralls --config_file .coverage.rc
  - coveralls

addons:
  apt:
    packages:
      - libgstreamer0.10-0
      - libgstreamer-plugins-base0.10-0
      - gconf2
      - gconf2-common
      - libgconf-2-4
      - libgconf2-4
      - libgq-gconf-dev
      - libgq-gconf0
      - dpkg-dev
      - build-essential
      - libjpeg-dev
      - libtiff-dev
      - libgtk2.0-dev
      - libsdl1.2-dev
      - libgstreamer-plugins-base0.10-dev

#  artifacts: true
